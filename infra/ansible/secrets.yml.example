# OpenAI
openai_api_key: "sk-replace-me"
openai_model: "gpt-4o-mini"

# App
log_level: "INFO"
aws_region: "eu-west-3"

# Postgres
postgres_user: "transcription"
postgres_password: "transcription"
postgres_db: "transcription"

# Grafana
grafana_user: "admin"
grafana_password: "changeme-strong-password"

# Kubernetes registry pull credentials.
gitlab_registry_user: "my-gitlab-username"
gitlab_registry_token: "gldt-replace-me"
gitlab_registry_url: "registry.gitlab.com"

# Image to deploy. After CI builds, this is registry.gitlab.com/<namespace>/<project>:<tag>
api_image: "registry.gitlab.com/my-namespace/letter-transcription-mlops:latest"