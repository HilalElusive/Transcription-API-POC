# Copy to terraform.tfvars (gitignored) and set values.

kubectl_allowed_cidr = "MY.PUBLIC.IP/32"

# Optional overrides:
# instance_type       = "t3.large"
# root_volume_size_gb = 30
# ssh_allowed_cidr    = "0.0.0.0/0"